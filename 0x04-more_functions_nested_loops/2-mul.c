#include "main.h"

/**
 * mul - to check for a digit 0 through 9
 * @a: first number to multiply
 * @b: second number to multiply
 * Return: 1 if digit, 0 otherwise
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
