#include <stdio.h>

/**
 * main - print the progra's name
 * @argc: the number of string arguments
 * @argv: the array of pointers to the string arguments
 *
 * Return: Always 0, if success
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

